import style from "./Main.module.scss"

export const Main = () => {

    return (
        <main className={style.mainStyle}>
            <h1 className={style.fontStyle}>This is Main</h1>
        </main>
    );

}